The data directory
------------------

<clientID>.json files contain the Request data being submitted in files named after the specific contact's ID#.

status.log provides information about whether the final submission of returned data is successful.